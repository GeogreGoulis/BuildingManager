<!DOCTYPE html>
<html lang="el">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{documentTitle}}</title>
  <style>
    {{{styles}}}
  </style>
</head>
<body>
  <div class="document">
    <!-- HEADER -->
    <header class="header">
      <div class="header-top">
        <div class="building-info">
          <div class="building-name">{{building.name}}</div>
          <div class="building-address">{{building.address}}</div>
          {{#if building.taxId}}
          <div class="building-address">ΑΦΜ: {{building.taxId}}</div>
          {{/if}}
        </div>
        <div class="document-info">
          <div><strong>Έκδοση:</strong> {{version}}</div>
          <div><strong>Ημ/νία Έκδοσης:</strong> {{generatedAt}}</div>
          {{#if isLocked}}
          <div class="locked-indicator">ΚΛΕΙΔΩΜΕΝΗ</div>
          {{/if}}
        </div>
      </div>
      
      <div class="document-title">{{documentTitle}}</div>
      
      {{#if period}}
      <div class="period-info">
        Περίοδος: {{period.month}}/{{period.year}}
      </div>
      {{/if}}
    </header>

    <!-- MAIN CONTENT -->
    <main>
      {{{content}}}
    </main>

    <!-- FOOTER -->
    <footer class="footer">
      <div class="footer-left">
        Σελίδα {{pageNumber}} από {{totalPages}}
      </div>
      <div class="footer-center">
        {{building.name}}
      </div>
      <div class="footer-right">
        {{generatedAt}}
      </div>
    </footer>

    <!-- METADATA (for auditing) -->
    <div class="metadata">
      <div class="version-info">
        <span>Template: {{templateVersion}}</span>
        <span>Data Hash: {{dataHash}}</span>
        <span>Generated By: Building Manager v1.0</span>
      </div>
    </div>
  </div>
</body>
</html>
